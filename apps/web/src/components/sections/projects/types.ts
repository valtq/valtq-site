/**
 * UI chrome strings for the projects section. Structurally satisfied by
 * `dict.projects`, so a dictionary slice can be passed straight through.
 */
export interface ProjectLabels {
  featuredLabel: string;
  techLabel: string;
  viewProject: string;
  viewSource: string;
  viewCaseStudy: string;
  moreTech: string;
  opensInNewTab: string;
}
